wiki_transform:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('DBT_HOST', 'localhost') }}"
      port: 5432
      user: pipeline
      password: pipeline
      dbname: wiki
      schema: analytics
      threads: 4